const defaultMessage: string =
  "Hola, quiero crear una cuenta en Aria Club! Mi nombre es:  ";

const defaultEncodedMessage: string = encodeURIComponent(defaultMessage);

export default defaultEncodedMessage;
